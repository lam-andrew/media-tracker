name: Security

# Secret, dependency, and filesystem scanning. Complements CodeQL (SAST) and Dependabot
# (update PRs) with fail-on-detection gates and a weekly scheduled sweep.
on:
  push:
  pull_request:
    branches: [main]
  schedule:
    - cron: "23 4 * * 1" # Mondays 04:23 UTC

permissions:
  contents: read

jobs:
  secret-scan:
    name: Secret scan (gitleaks)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0 # full history so gitleaks can scan past commits
      - name: Run gitleaks
        uses: gitleaks/gitleaks-action@v2
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  deps:
    name: Dependency audit (npm audit)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm
      - run: npm ci
      # Gate on vulnerabilities in shipped (production) dependencies. Dev-only tooling
      # advisories remain visible via Dependabot and Trivy without blocking the pipeline.
      - name: Audit production dependencies (fail on high+)
        run: npm audit --omit=dev --audit-level=high

  trivy:
    name: Filesystem scan (Trivy)
    runs-on: ubuntu-latest
    permissions:
      contents: read
      security-events: write
    steps:
      - uses: actions/checkout@v4
      - name: Trivy filesystem scan → SARIF
        uses: aquasecurity/trivy-action@0.35.0
        with:
          scan-type: fs
          scanners: vuln,secret,misconfig
          severity: CRITICAL,HIGH
          format: sarif
          output: trivy-results.sarif
      # SARIF upload requires a public repo or GitHub Advanced Security. On a private repo
      # this step is a no-op-ish failure, so it must not fail the pipeline.
      - name: Upload results to the Security tab
        uses: github/codeql-action/upload-sarif@v4
        if: always()
        continue-on-error: true
        with:
          sarif_file: trivy-results.sarif
